# Серверный пин (ровно 6 цифр). Не используй NEXT_PUBLIC_ — иначе код уйдёт в браузер.
# Скопируй в .env.local и подставь свой код (файл .env.local в git не коммитится).
VIKA_LOVE_PIN=

# --- Email notifications (Vercel Cron -> /api/cron/anniversary) ---
# One secret used to protect the cron endpoint.
CRON_SECRET=

# SMTP (Yandex example)
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=

EMAIL_FROM=
EMAIL_TO=griegafem@gmail.com,viktoriyatsmakova@yandex.ru
